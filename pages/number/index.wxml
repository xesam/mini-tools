<view>
    <view class="items">
        <text class="item" wx:for="{{values}}" wx:key="index">{{item}}</text>
    </view>
    <view class="items">
        <text class="item" wx:for="{{units}}" wx:key="index">{{item}}</text>
    </view>
</view>